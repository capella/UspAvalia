# UspAvalia Environment Configuration
# Copy this file to .env and update with your actual values

# ===================================
# Server Configuration
# ===================================
USPAVALIA_SERVER_HOST=0.0.0.0
USPAVALIA_SERVER_PORT=8080
USPAVALIA_SERVER_URL=http://localhost:8080

# ===================================
# Database Configuration
# ===================================

# SQLite (default - recommended for development)
USPAVALIA_DATABASE_TYPE=sqlite
USPAVALIA_DATABASE_PATH=./uspavalia.db

# MySQL (uncomment for production)
# USPAVALIA_DATABASE_TYPE=mysql
# USPAVALIA_DATABASE_HOST=localhost
# USPAVALIA_DATABASE_PORT=3306
# USPAVALIA_DATABASE_USER=uspavalia
# USPAVALIA_DATABASE_PASSWORD=your-mysql-password
# USPAVALIA_DATABASE_NAME=uspavalia

# ===================================
# Security Configuration
# ===================================

# Generate secure keys for production:
# openssl rand -base64 32

USPAVALIA_SECURITY_SECRET_KEY=change-this-to-a-random-32-byte-secret-key
USPAVALIA_SECURITY_CSRF_KEY=change-this-to-another-random-32-byte-key
USPAVALIA_SECURITY_MAGIC_LINK_HMAC_KEY=change-this-to-random-hmac-key-32-bytes
USPAVALIA_SECURITY_SESSION_NAME=uspavalia_session

# hCaptcha Configuration (optional - for bot protection)
# Sign up at https://www.hcaptcha.com/
USPAVALIA_SECURITY_HCAPTCHA_SITE_KEY=your-hcaptcha-site-key
USPAVALIA_SECURITY_HCAPTCHA_SECRET_KEY=your-hcaptcha-secret-key

# ===================================
# OAuth Configuration
# ===================================

# Google OAuth (for social login)
# Get credentials at https://console.cloud.google.com/
USPAVALIA_OAUTH_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
USPAVALIA_OAUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret
USPAVALIA_OAUTH_GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

# ===================================
# Email Configuration (SendGrid)
# ===================================

# Sign up at https://sendgrid.com/
USPAVALIA_EMAIL_SENDGRID_API_KEY=your-sendgrid-api-key
USPAVALIA_EMAIL_FROM_EMAIL=noreply@uspavalia.com
USPAVALIA_EMAIL_FROM_NAME=USP Avalia

# ===================================
# Old Database (for migration only)
# ===================================

# Configuration for importing data from legacy PHP/MySQL database
# Only needed if running: ./uspavalia import-old-data
USPAVALIA_OLD_DATABASE_TYPE=mysql
USPAVALIA_OLD_DATABASE_HOST=localhost
USPAVALIA_OLD_DATABASE_PORT=3306
USPAVALIA_OLD_DATABASE_USER=uspavalia
USPAVALIA_OLD_DATABASE_PASSWORD=your-old-db-password
USPAVALIA_OLD_DATABASE_NAME=uspavalia_old

# ===================================
# Docker Compose Variables
# ===================================

# These are used by docker-compose.yml
# (Shorter variable names for convenience)

SERVER_URL=http://localhost:8080

# Security Keys
SECRET_KEY=change-this-secret-key-for-production
CSRF_KEY=change-this-csrf-key-for-production
MAGIC_LINK_HMAC_KEY=change-this-hmac-key-for-production

# hCaptcha
HCAPTCHA_SITE_KEY=your-hcaptcha-site-key
HCAPTCHA_SECRET_KEY=your-hcaptcha-secret-key

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

# SendGrid
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@uspavalia.com

# MySQL (if using MySQL with Docker Compose)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_PASSWORD=uspavaliapassword

# ===================================
# Production Notes
# ===================================

# 1. Generate secure random keys:
#    openssl rand -base64 32

# 2. Never commit .env file to version control

# 3. Use HTTPS in production (configure via reverse proxy)

# 4. Enable hCaptcha to prevent bot abuse

# 5. Configure proper email service (SendGrid)

# 6. Use MySQL for production (better performance than SQLite)

# 7. Set proper SERVER_URL for your domain
